# Test and lint dependencies — NEVER copied into a service image.
# Runtime dependencies live in <service>/requirements.txt.
pytest==8.3.4
# Async transport clients mean async tests; "auto" mode is configured in pyproject.toml.
pytest-asyncio==0.25.3
ruff==0.9.2
# TestClient needs an HTTP client; Starlette >= 1.3 deprecates httpx in favour of httpx2
# (keeping httpx installed makes every test run emit a StarletteDeprecationWarning).
httpx2==2.9.1
# The config plumbing test reads the compose files as DATA (no Docker, no shelling out), so it
# needs a YAML parser. Test-only: no service imports it.
pyyaml==6.0.2
# Charts for measurement reports (scripts/plot_threshold.py -> docs/*.png). Used by repo-level
# scripts only, never by a service or a test — a report figure has to be reproducible, so the
# dependency is declared rather than installed ad hoc.
matplotlib==3.11.1
