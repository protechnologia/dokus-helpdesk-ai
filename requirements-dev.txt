# Test and lint dependencies — NEVER copied into a service image.
# Runtime dependencies live in <service>/requirements.txt.
pytest==8.3.4
ruff==0.9.2
# TestClient needs an HTTP client; Starlette >= 1.3 deprecates httpx in favour of httpx2
# (keeping httpx installed makes every test run emit a StarletteDeprecationWarning).
httpx2==2.9.1
