# Runtime dependencies of the `api` service — these go INTO the image.
# Test/lint tooling lives in requirements-dev.txt at the repo root and never enters the image.
pydantic==2.10.5
pydantic-settings==2.7.1
fastapi==0.140.0
uvicorn[standard]==0.51.0
typer==0.27.0
# Claude API SDK. Imported by exactly one module (app/llm/client_claude.py) — CLAUDE.md rule 4.
anthropic==0.120.2
# OpenAI SDK. Imported by exactly one module (app/llm/client_openai.py) — CLAUDE.md rule 4. Also the
# route for OpenAI-compatible endpoints (Bielik on RunPod, Ollama, vLLM) via LLM_BASE_URL.
openai==2.52.0
